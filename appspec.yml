
version: 0.0
os: linux
files:
  - source:  /
    destination: /home/ubuntu/
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: scripts/application_start.sh
      timeout: 60
      runas: user-name
