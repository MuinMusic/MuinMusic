
version: 0.0
os: linux
#files:
#  - source:  /
#    destination: /home/ubuntu/
#    overwrite: yes
files:
  - source: /
    destination: /var/www/myapp

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu


hooks:
  ApplicationStart:
    - location: deploy.sh
      timeout: 300
      runas: ec2-user
