spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost:3306/muin_music
    username: muin_music
    password: muin1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
